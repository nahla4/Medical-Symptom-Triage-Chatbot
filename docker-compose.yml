version: '3.8'

services:
  medical-triage-chatbot:
    build: .
    container_name: medical_triage_chatbot
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    restart: unless-stopped
